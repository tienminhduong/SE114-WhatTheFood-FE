package com.example.se114_whatthefood_fe.view.map

import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.navigation.NavController
import androidx.navigation.NavHostController

@Composable
fun MapScreen(
    modifier: Modifier,
    navHostController: NavHostController
) {
    // This is a placeholder for the MapScreen composable.
    // The actual implementation will include the map view and any other necessary UI components.
    MapViewContainer(modifier = modifier)
}